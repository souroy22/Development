const initialState = {

}

export const cart = (state=initialState, action) => {
     switch (action.type) {
          case '':
               
               break;
     
          default:
               return state;
     }
}