const initialState = {
     wishlistProducts: []
}

export const wishlist = (state=initialState, action) => {
     switch(action.type) {
          

          default:
               return state;
     }
}